{
  "name": "root",
  "private": true,
  "scripts": {
    "build-netlify": "npm run components:run -- build-stencil && npm run components:run -- build-storybook",
    "components:run": "lerna run --scope @core-design/components"
  },
  "devDependencies": {
    "lerna": "^3.22.0"
  }
}
